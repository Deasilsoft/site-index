# Copilot Instructions

## Codebase overview workflow

- If `code-slicer` is available, use it first to inspect a file along with its dependencies.
- Use direct file reads only for focused follow-ups after the initial overview.
- If `code-slicer` is unavailable, proceed with standard file-by-file exploration.
