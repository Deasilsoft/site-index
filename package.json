{
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/site-index",
    "packages/@site-index/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "clean": "npm run clean:install && npm run clean:packages",
    "preclean:install": "rm -rf node_modules package-lock.json",
    "clean:install": "npm install",
    "clean:packages": "npm run clean --workspaces --if-present",
    "cli": "npm run cli -w site-index --",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "knip": "knip",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.0.0",
    "code-slicer": "^0.5.0",
    "eslint": "^10.5.0",
    "eslint-plugin-unicorn": "^70.0.0",
    "knip": "^6.17.1",
    "prettier": "^3.8.4",
    "prettier-plugin-packagejson": "^3.0.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1"
  },
  "engines": {
    "node": ">=22"
  }
}
